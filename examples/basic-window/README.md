# Basic Window Example

This folder is reserved for a minimal working Re:UI window example.

Keep examples aligned with the current public API. Do not publish speculative code that does not match the framework source.
